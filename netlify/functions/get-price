import fetch from 'node-fetch'

export const handler = async () => {
  const POKE_API = 'https://httpbin.org/get'

  const response = await fetch(POKE_API)
  const data = await response.json()

  return {
    statusCode: 200,
    body: JSON.stringify({
      data
    })
  }
}